<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">

    <insert id="insert" parameterType="Orders" useGeneratedKeys="true" keyProperty="id">
        insert into orders
        (number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status, amount, remark,
         phone, address, consignee, estimated_delivery_time, delivery_status, pack_amount, tableware_number,
         tableware_status,submit_request_id)
        values (#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod},
                #{payStatus}, #{amount}, #{remark}, #{phone}, #{address}, #{consignee},
                #{estimatedDeliveryTime}, #{deliveryStatus}, #{packAmount}, #{tablewareNumber}, #{tablewareStatus}, #{submitRequestId})
    </insert>

    <update id="updateIfStatus">
        update orders
        <set>
            <if test="orders.cancelReason != null and orders.cancelReason != ''">
                cancel_reason = #{orders.cancelReason},
            </if>
            <if test="orders.rejectionReason != null and orders.rejectionReason != ''">
                rejection_reason = #{orders.rejectionReason},
            </if>
            <if test="orders.cancelTime != null">
                cancel_time = #{orders.cancelTime},
            </if>
            <if test="orders.payStatus != null">
                pay_status = #{orders.payStatus},
            </if>
            <if test="orders.payMethod != null">
                pay_method = #{orders.payMethod},
            </if>
            <if test="orders.checkoutTime != null">
                checkout_time = #{orders.checkoutTime},
            </if>
            <if test="orders.status != null">
                status = #{orders.status},
            </if>
            <if test="orders.deliveryTime != null">
                delivery_time = #{orders.deliveryTime},
            </if>
        </set>
        where id = #{orders.id}
          and status = #{expectedStatus}
    </update>

    <select id="cancelOrderByStatusAndTime" resultType="com.sky.entity.Orders">
        select * from orders where status = #{pendingPayment} and order_time &lt; #{time}
    </select>

    <select id="pageQuery" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="number != null and number != ''">
                and number = #{number}
            </if>
            <if test="phone != null and phone != ''">
                and phone like concat('%', #{phone}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="beginTime != null">
                and order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt;= #{endTime}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
        </where>
        order by order_time desc
    </select>

    <select id="getStatistics" resultType="com.sky.vo.OrderStatisticsVO">
        select
            sum(status = 2) as toBeConfirmed,
            sum(status = 3) as confirmed,
            sum(status = 4) as deliveryInProgress
        from orders
        where status in (2, 3, 4)
    </select>

    <select id="getSumValue" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <select id="getOrderCount" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="getSalesTop10" resultType="com.sky.dto.GoodsSalesDTO">
        select od.name, sum(od.number) as number
        from order_detail od, orders o
        where o.id = od.order_id
          and o.status = 5
          and o.order_time &gt;= #{begin}
          and o.order_time &lt; #{end}
        group by od.name
        order by number desc
        limit 10
    </select>

</mapper>
